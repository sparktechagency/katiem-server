export enum APPLICATION_STATUS {
    PENDING = 'pending',
    APPROVED = 'approved',
    DECLINED = 'declined',
}

export enum BOOKING_STATUS {
    PENDING = 'pending',
    APPROVED = 'approved',
    DECLINED = 'declined',
}