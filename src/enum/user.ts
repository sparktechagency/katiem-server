export enum USER_ROLES {
    ADMIN = 'admin',
    EMPLOYER = 'employer',
    GUEST = 'guest',
    WORKER = 'worker',
}


export enum USER_STATUS {
    ACTIVE = 'active',
    RESTRICTED = 'restricted',
    DELETED = 'deleted',
}